# AndroidX Support (required for Compose, Hilt, etc.)
android.useAndroidX=true

# Jetifier for legacy library compat (optional but recommended)
android.enableJetifier=true

# Suppress compileSdk=35 warning (AGP 8.5.1 compatibility)
android.suppressUnsupportedCompileSdk=35

# Other common settings (add if missing; safe defaults)
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official